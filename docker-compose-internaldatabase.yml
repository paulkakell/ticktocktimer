version: '3.8'

services:
  ticktoctimerapp:
    image: paulkakell/ticktocktimer:v0.1.0-alpha
    container_name: ticktoctimer_app
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
