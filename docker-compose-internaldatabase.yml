version: '3.8'

services:
  ticktoctimerapp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ticktoctimer_app
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
